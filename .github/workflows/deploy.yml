jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read # Ajout nécessaire pour permettre à l'action de lire le code
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }} # Utilisation du token automatique
      # ... le reste de votre configuration FTP/SFTP
